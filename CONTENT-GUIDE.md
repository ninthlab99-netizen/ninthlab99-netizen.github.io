# 內容架構說明（給未來管理這個網站的人或 AI 助理看）

這份文件不會顯示在網站上，純粹是給「以後要修改這個網站的人（包括你自己，或未來協助你的 Claude）」快速搞懂架構用的。

## 核心原則

這個網站的**文字內容**和**網站程式**是分開的：

- 網站程式（HTML／CSS／JavaScript）幾乎不太需要再改，它只負責「怎麼把內容排版出來」
- 所有會變動的文字、價格、服務項目、公告，全部放在 `content/` 資料夾底下的幾個 JSON 檔案裡

任何人（或 AI）要修改網站內容，**只需要編輯 `content/` 裡對應的檔案**，不需要碰 `index.html`、`assets/css/style.css`、`assets/js/main.js` 這幾個程式檔案，除非是要新增全新的功能區塊。

## 各檔案對應網站的哪個部分

| 檔案 | 內容 | 何時要改它 |
|---|---|---|
| `content/site.json` | 品牌名稱（中英文）、電話、LINE 帳號、營業時間、服務地區、SEO 標題描述 | 改聯絡資訊、換 LINE 帳號、補營業時間 |
| `content/homepage.json` | 首頁 Hero 文字、服務選擇器文字、維修流程六步驟、到府服務文字、信任區文字 | 改首頁標語、改流程步驟 |
| `content/services.json` | 核心三大分類（iPhone／Android／電腦）與其他服務（平板／音響／其他3C）的所有維修項目 | 新增/刪除/修改維修項目、換分類示意圖 |
| `content/faq.json` | 常見問題列表 | 新增/刪除/修改 FAQ |
| `content/contact.json` | 聯絡客服區塊的標題與按鈕文字 | 改聯絡區塊文案 |
| `content/announcements.json` | 公告清單，預設是空的 | 要發布新公告時，在 `items` 陣列裡新增一筆 |
| `content/reviews.json` | Google 評論整合的開關與設定，目前 `enabled` 是 `false` | 之後申請到 Google Place ID，把它填進去並把 `enabled` 改成 `true` |

## 修改時的重要規則

1. **維修項目、FAQ 都沒有固定價格寫死在檔案裡**——網站的設計是引導客戶「選擇故障狀況 → LINE 詢問」，不是顯示價目表。如果未來要顯示具體價格，需要在 `services.json` 的每個項目底下新增一個 `price` 欄位，並同步修改 `assets/js/main.js` 裡負責產生 LINE 訊息文字的 `buildLineMessage()` 函式，讓價格也一併帶入訊息。
2. **每筆資料盡量用 `id` 辨識，不要用「第幾筆」辨識**——例如 `services.json` 裡的三大分類都有 `id: "iphone"` / `"android"` / `"computer"`，未來如果要精準修改某一個分類，指定 `id` 比數「第幾個」更不容易改錯。
3. **JSON 格式規則**：每一行結尾除了最後一項，其他都要有逗號 `,`；文字都要用雙引號 `"` 包起來；改完最好用線上工具（例如搜尋「JSON validator」）貼上去檢查格式有沒有錯，格式壞掉會導致整個網站讀不到內容。
4. **不要在任何內容檔案裡寫入真實客戶的個人資料**（姓名、電話、對話紀錄），這些檔案會被公開存放在 GitHub 上，任何人都看得到。

## 給未來的 AI 助理（包括之後接手的 Claude）

如果你是被要求「修改這個網站」的 AI，建議的操作方式：

1. 先讀這份 `CONTENT-GUIDE.md`，了解要改的內容在哪個檔案
2. 只編輯 `content/` 資料夾裡對應的 JSON 檔案，不要動 `index.html`／CSS／JS，除非使用者明確要求新增功能
3. 改完用 `python3 -c "import json; json.load(open('檔案路徑'))"` 這類指令確認 JSON 格式沒壞掉，再交給使用者上傳
4. 每次只改使用者要求的那個檔案，不要順便「順手」修改其他沒被要求的內容
5. 這個網站目前是純靜態架構（沒有資料庫、沒有後端伺服器），任何「即時互動」（例如自動回覆訊息、自動同步評論）都需要另外串接外部服務，不是改 JSON 檔案就能做到的，遇到這類需求要老實跟使用者說明，不要假裝用 JSON 檔案能做到即時功能
